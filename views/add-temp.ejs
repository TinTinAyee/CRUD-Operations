<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title> <%= pageTitle %> </title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">
 
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">

    <style>

        body{
            background-color: #e9ebee;
        }
        .container{
            display: block;
        }
        .card{
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0px 5px 10px rgb(155,160,185), 0px 15px 40px rgb(166,173,201);
            border:none;
            height: 300px;
        }

        .result{
            font-size: 21px;
        }
        label{
            font-size: 20px;
        }
        a{
            color: #ffffff!important;
        }

        .title{
            display: flex;
            flex-direction: row;
        }
        
    </style>
</head>
<body>

<section>
    <div class="container">
        <div class="row justify-content-center" style="width:200px auto; margin:80px 0px;">
            <div class="col-md-6 card ">
                <div class="title justify-content-center mt-3">
                    <form action="/" method="GET">
                        <button type="submit" class="btn btn-primary mr-2" style="margin-right: 20px;"> 
                            <i class="fas fa-list"></i> Temperature List
                        </button>
                    </form>
                    <form action="/add-temp" method="GET">
                        <button type="submit" class="btn btn-success ml-2">
                            <i class="fas fa-plus"></i> Add Temperature 
                        </button>
                    </form>
                </div>    
                    <div class="row">
                        <div class="col-md-7 offset-md-3" style="margin-left: 100px;">
                            <div class="from-group mt-3">
                                <form action="<% if(editing) { %> /edit-temp <%}else { %>/add-temp <% } %>" method="POST">

                                    <label for="temp"> <h3>Add Temperature </h3>  </label>
                                    <div class="input-group">
                                        <input 
                                            type="number"
                                            name="tempDegree" 
                                            id="temp" 
                                            style="width :300px;"
                                            value ="<% if(editing) {%> <%= temp.tempDegree %> <% } %>">
                                    </div>
                                    <% if(editing) { %>
                                        <input type="hidden" name="tempId" value="<%= temp._id %>">
                                    <% } %>

                                    <button type="submit" class="btn btn-info float-right mt-3 mb-3"> <% if(editing) { %> Update <% } else { %> Add <%}%>
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
            </div>
        </div>
    </div>
</section>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-gtEjrD/SeCtmISkJkNUaaKMoLD0//ElJ19smozuHV6z3Iehds+3Ulb9Bn9Plx0x4" crossorigin="anonymous"></script>

</body>
</html>